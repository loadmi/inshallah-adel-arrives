# Server Configuration
PORT=3000
NODE_ENV=development

# Database
DB_PATH=./data/adel-data.db

# ML Model
MODEL_PATH=./data/models
MIN_TRAINING_SAMPLES=10
TRAINING_EPOCHS=100
BATCH_SIZE=16

# CORS
# For multiple origins, separate with commas: http://localhost:4200,https://inshallah-adel-arrives.xyz
CORS_ORIGIN=http://localhost:4200,https://inshallah-adel-arrives.xyz

# Delete Protection
DELETE_PASSWORD=changeme

# Logging
LOG_LEVEL=info
